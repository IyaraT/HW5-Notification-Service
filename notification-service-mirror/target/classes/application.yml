spring:
  application:
    name: notification-service-mirror
  config:
    import: "optional:configserver:http://localhost:8088"
  kafka:
    consumer:
      group-id: notification-v2

eureka:
  client:
    service-url:
      defaultZone: "http://localhost:8761/eureka/"